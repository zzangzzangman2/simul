# SIMUL 저장소 작업 규칙

최종 갱신: 2026-08-04

이 파일은 현재 유효한 전역 규칙만 담는다. 과거 후보, 제작 과정, 폐기된 설정은
규칙으로 취급하지 않는다.

## 작업 전 읽는 순서

1. `DOCUMENTATION_INDEX.md`
2. `PROJECT_GUIDE.md`
3. 작업 분야의 정본 문서
4. 관련 코드와 테스트

문서와 코드가 다르면 임의로 한쪽을 따르지 말고 현재 런타임 동작과 테스트를
확인한 뒤 같은 변경에서 문서와 코드를 함께 맞춘다. `DATA_SOURCES.md`의 출처와
라이선스 규칙은 삭제하거나 약화하지 않는다.

## 제품과 화면

- 제품은 2000년 서울을 배경으로 한 세로형 모바일 투자·생활 시뮬레이션이다.
- 모든 모바일 Web은 `390×844` 하나의 논리 화면으로 렌더링한 뒤 기기 가용
  뷰포트 안에 종횡비를 유지해 동일 비율로 확대·축소한다. 폴드·플립·일반폰마다
  인물·HUD·대사창 위치를 따로 바꾸지 않는다. 남는 영역은 게임색 배경 확장부로
  채우고 밝은 기본 여백이나 잘린 UI를 노출하지 않는다.
- 최소 너비 360px에서 가로 스크롤이 없어야 한다.
- 데스크톱에서는 `hover: hover`, `pointer: fine`, CSS 너비 700px 이상이며 터치
  입력이 없을 때만 같은 `390×844` 스테이지에 전화기 테두리를 추가한다. S펜이나
  터치 입력이 있는 폴드를 데스크톱으로 오인하면 안 된다.
- 기본 글꼴은 Maplestory이며, 폰트 로딩 실패에 대비한 Pretendard 계열 폴백을
  유지한다.
- 루트 `/`는 쿼리와 해시를 보존해 `/play/index.html`로 이동한다. 같은 Wi-Fi의
  모바일은 `http://<PC 내부 IP>:8000`만 입력해 게임에 들어갈 수 있어야 한다.
- 배포 구조인 `.openai/hosting.json`, `public/index.html`, `public/play/`를
  임의로 삭제하지 않는다. `public/play/`는 Flutter Web 빌드 산출물이며 직접
  편집하지 않고 소스 수정 후 다시 빌드한다.

## 세계관과 시작 상태

- 유일한 세계관 정본은 `DECIMAL_WORLD.md`다.
- 주인공 플레이어와 김학준, 여학생 8명은 1987년생 동갑이며 1999년 프로젝트 데시멀의
  비밀 선발을 통과한 최종 열 명이다. 보호자·가족·용돈·가정 계좌를 새로 붙이지 않는다.
- 학교, 입학, 학년, 선배·후배, 미래양성원, 기수, SEED 설정은 폐기됐다. 열 명은
  `동기`, 생활 공간은 `강남 아지트` 또는 `데시멀 센터`로 부른다.
- 최종 도착은 `1999-12-31`, 정식 튜토리얼·자유 플레이 상태는
  `2000-01-03 09:00`에 50,000원 모의계좌로 딱 하루 연습하고 `2000-01-04`부터 같은 50,000원으로 실전 거래를 시작한다.
- 확정 순이익의 20%는 국가 환수금, 80%는 자립적립금으로 기록한다.
- 저장 스키마는 `GameState.schemaVersion == 27`이다. 최대 5슬롯을 유지하고 기존
  단일 저장은 1번 슬롯으로 보존한다. 날짜가 넘어갈 때와 주식 화면을 정상적으로
  닫을 때 활성 슬롯을 안전하게 저장한다.

## 프롤로그와 생활 흐름

- 시작 순서는 `플레이어 이름 확정 → 1999년 국정원 데시멀 재가동 → 봉인된 실패 기록 →
  1999년 행렬 시험 → 불공정 게임 → 결핍·욕망 검증 → 1999년 12월 강남 아지트 →
  첫날 공동생활 → 2000년 1월 3일 주식 PC 실습`이다.
- 정규 대사본과 버전 번호는 정본 JSON을 기준으로 하며 장면 상한은 320이다.
- 프롤로그 건너뛰기는 현재 장만 넘기고 다음 장 첫 장면에서 멈춘다. 마지막
  건너뛰기도 PC 전원, 앱 실행, 저장 생성을 대신 수행하지 않는다.
- 열 명은 생활 라운지를 공유하되 개인 수면칸·사물함과 잠금 가능한 탈의·세면
  칸막이로 경계를 지킨다.
- 프롤로그 첫 화면에서 사용자가 플레이어 이름을 확정한다. 1월 3일 각자 꺼진 CRT
  PC 한 대를 배정하고, 플레이어가 전원을 켜서 `주식실습`을 연 뒤 운용장부 이름을
  확정해야 프롤로그 체크포인트가 정식 국가계좌
  저장으로 전환되고 한빛통신 매수·가격 변화·매도 튜토리얼이 시작된다.

## 인물 정본

여학생 8명, 김학준, 한서윤 운영관만 생활 허브의 고정 `캐릭터` 화면 대상이다.
범용 `여자친구`, `여자1`, 미연결 연애 NPC를 다시 만들거나 연결하지 않는다.

| 인물 | MBTI | 정체성 앵커 |
| --- | --- | --- |
| 김서아 | ISFJ | `art_references/kim_seoa_identity_anchor_v2.png` |
| 이지안 | ISTP | `art_references/lee_jian_face_identity_anchor_v3.png`, `lee_jian_identity_anchor_v3.png` |
| 최이서 | ISFP | `art_references/choi_iseo_identity_anchor_v1.png` |
| 정아린 | ESTJ | `art_references/jung_arin_identity_anchor_v1.png` |
| 박하은 | ENFJ | `art_references/park_haeun_face_identity_anchor_v3.png`, `park_haeun_identity_anchor_v3.png` |
| 한수아 | ENFP | `art_references/han_sua_identity_anchor_v3.png`(1차 전신), `art_references/han_sua_face_identity_anchor_v3.png`(2차 얼굴 크롭) |
| 오지우 | ENTP | `art_references/oh_jiwoo_identity_anchor_v2.png` |
| 윤채아 | INTJ | `art_references/yoon_chaea_identity_anchor_v1.png` |
| 김학준 | ISTJ | `production_soft_painted/kim_hakjun/` v4 9종, `art_references/future_development_male_uniform_length_reference_v1.png` |
| 한서윤 | INFJ | 외형 비보존. 23세 운영관 역할에서 v3 새 디자인 승인 필요 |

- 한수아는 전신 앵커를 1차 정체성 기준으로 사용하고 얼굴 크롭 앵커는 얼굴 비율을
  확인하는 2차 기준으로만 사용한다. 얼굴 크롭의 `1/2` UI, 보이는 손, 파란 천·레이스는
  정체성 요소가 아니므로 생성·수정 결과에 복제하지 않는다.
- 동기 10명은 14살, 한서윤은 23세다. 인물 UI와 대사에서는 한국식 나이 표기인
  `14살`을 사용하고, 진행 연도에 맞춰 한국식 연 나이를 갱신한다. 생일 정본은
  김학준 1987.01.17, 김서아 1987.02.12, 이지안 1987.03.28, 최이서 1987.05.04,
  정아린 1987.06.21, 박하은 1987.08.08, 한수아 1987.09.16, 오지우 1987.11.03,
  윤채아 1987.12.19, 한서윤 1978.04.09다.
- 플레이어의 대사는 이름과 텍스트만 표시하고 인물 초상화·레이어는 사용하지 않는다.
  플레이어 외형은 사용자의 상상에 맡기며 편집기에서도 `인물 없음`만 선택한다.
- `flutter_app/assets/images/protagonist_seed01/`의 구형 포즈 24종은 전부 삭제했다.
  플레이어를 NPC와 구분되는 1인칭 선택 주체로 남기기 위한 제품 결정이므로 기본
  아바타·실루엣·다른 인물 이미지로 대체하거나 해당 경로를 복원하지 않는다.
- 성격과 대사는 `DECIMAL_WORLD.md`, `characters/cohort6_girls/README.md`와 각 인물
  문서를 함께 따른다.
  MBTI는 반응 경향을 잡는 보조 도구일 뿐 지능·도덕성·능력·행동을 고정하지 않는다.
- `캐릭터` 화면은 카드 첫 진입부터 현재 나이·생일·현재 호감도·고유 능력·총자산과
  자금 위치를 보여 준다. 초상화를 누르면 MBTI, 성격, 좋아하는 것, 강점, 투자 기준,
  관계 방식과 같은 자산 장부를 보여 주는 상세 화면으로 연결한다. 한서윤은 10인 투자
  참가자가 아니므로 개인 자산 비공개를 명시한다.
- 여학생 관계는 8명만 호감도·신뢰·친밀·투자존중을 각각 1~100으로 저장한다. 같은 날 중복 관계 행동을 허용하지
  않으며 14살 또래의 안전한 공개 장소 교류만 사용한다.

## 캐릭터 이미지 규칙

공식 목표 화풍은 오직 `SIMUL polished soft-render VN anime v3`다. 생성·수정 전
다음을 직접 연다.

1. `IMAGE_GENERATION_STYLE_PROMPT.md`
2. `ART_STYLE_GUIDE.md`
3. `ART_STYLE_AUDIT.md`
4. `art_references/simul_polished_soft_render_vn_style_anchor_v3.png` — 2026-08-03 사용자 승인 v3 화풍 정식 앵커
5. 여자 동기 8명인 경우에만 해당 인물의 승인 정체성 앵커와
   `flutter_app/assets/images/production_soft_painted/<인물>/`

- v3 정식 앵커는 얇고 또렷한 짙은 유색선, 깊이 있는 부드러운 명암, 선명한 다층 홍채,
  매끈하지만 입체적인 피부, 고밀도 머릿결과 통제된 윤광, 또렷한 의상 재질 문법만
  전달한다. 앵커 속 한수아의 얼굴·검은 웨이브 머리·체형·현장복·포즈를 다른 인물에게
  복제하지 않는다.
- 실사·사진 합성·3D·치비·균일한 굵은 검정선·평평한 하드 셀 명암·흐린 페인터리
  번짐은 금지한다.
- 후보·아카이브·`draft`·`discarded` 경로는 신규 생성 참조가 아니다.
- **기존 얼굴·정체성·헤어·의상 보존 대상은 여자 동기 8명뿐이다.** 이들은 기존
  표정·포즈·카메라·프레이밍까지 유지하고 렌더링 화풍만 v3로 바꾼다.
- 주인공은 고정 인물 이미지를 사용하지 않는다. 김학준·한서윤·국정원/정부/청와대
  관계자 등 그 밖의 NPC는 별도 승인 규칙을 따른다. **윤하린 은행원과 서하늘
  공인중개사는 기존 얼굴·헤어·체형·의상·장신구·직업 소품을 정체성 앵커로 그대로
  유지하고 선화·채색·명암·재질감만 v3 렌더링으로 전환한다.** 두 인물을 새 얼굴이나
  새 의상으로 전면 재설계하지 않는다.
- 김학준은 전면 재설계를 마쳤다. 이후 포즈는 현행 v4 9종의 얼굴·헤어·체형과 공식
  남자 데시멀 센터 지급 현장복을 고정하며, 남자 현장복 앵커의 디자인·핏·비율·길이를 따른다.
- 여자 8명은 머리와 개별 사복을 가린 얼굴만 보아도, 얼굴을 가린 전신 실루엣만 보아도
  서로 구분되어야 한다. 얼굴 골격, 이마·헤어라인, 눈, 눈썹, 코, 입, 인중, 턱,
  볼 중 최소 네 축을 뚜렷하게 다르게 설계한다.
- 각 인물의 상대 키, 어깨 폭·경사, 몸통 길이, 허리·골반의 상대 폭, 팔다리 길이,
  건강한 다리 굵기, 체중 중심을 따로 정한다. 같은 마네킹에 얼굴만 바꾸지 않는다.
- 체형 차이는 건강하고 비선정적인 14살 범위에서만 표현한다.
- 여자 동기는 공통 교복이나 공통 센터 현장복을 사용하지 않는다. 인물별 사용자 승인
  사복을 얼굴·헤어·체형과 함께 정체성으로 고정한다. 2026-08-05 기준 김서아는 분홍
  퍼프소매 블라우스·검정 체크 치마·검정 메리제인, 오지우는 검정 오버핏 홈웨어와 검정
  슬라이드, 이지안은 흰색·남색 운동복과 운동화로 전환했다. 박하은은 긴 연회갈색
  머리·보라색 눈·하늘색 오버핏 니트·남색 체크 치마·맨발로 전환했다. 나머지 4명은 사용자가 새
  사복 참조를 줄 때까지 현재 승인 런타임 의상을 임시 유지하되,
  그 의상을 전원 공통 규칙이나 신규 캐릭터 템플릿으로 확장하지 않는다.
- 같은 인물의 표정·동작 세트는 얼굴, 체형, 복장, 소품을 고정하되 고개·시선·몸의
  기울기·손동작·머리카락 관성을 자연스럽게 달리한다.
- 여자 동기 8명과 1999년 국정원·정부 인물 6명은 v3 교체를 완료했다. 김학준의 새
  v3 대표 디자인도 런타임에 적용했으며 이후 포즈는 그 디자인을 기준으로 확장한다.
  다음 비보존 주요 인물과 반복 NPC도 세트 승인 전 기존 런타임 파일을 덮어쓰지 않는다.

## 게임 시스템 불변 조건

- 거래 대상은 50개 고정 가상기업으로 시작한다. 실제 기업명·제품명을 사용하지
  않는다. 사용자가 정한 회사 이름을 임의 접미사나 제품명으로 덮지 않는다.
- 시장은 08:00 이전 종가, 09:00 개장, 14:50~15:00 종가 동시호가, 15:00 마감,
  20:00까지 마감가 유지로 단순화한다. 주말·휴장일은 직전 종가를 유지한다.
- 시장·신문·부동산·사업은 저장된 월드시드 기반 결정론을 공유한다. 미래 사건,
  숨은 시나리오, 미래 가격을 조기 노출하지 않는다.
- 종목 상세의 주문 엔진은 매도·매수 각 10단계 깊이를 유지하되 모바일 화면은
  최우선부터 매도 7행·매수 7행만 한 화면에 표시한다.
  일반 주문 중 시장 시계와 호가는 계속 갱신한다.
- 현재가·최근 체결가는 실제 체결가이며 최우선 매도·매수 호가로 대체하지 않는다.
  호가 단위, 유동성, 중앙 경계 전진, 합성 체결의 정밀 규칙은 `BALANCE_NOTES.md`가
  정본이다. 엔진 수정 전에 해당 문서와 관련 회귀 테스트를 반드시 읽는다.
- 조간신문은 외부 AI·뉴스 API 없이 전날 공개 사실만으로 로컬 생성한다.
- 직접 `하루 보내기` 순서는 `거래일 15:00 종가·주식 손익 무화면 확정(휴장일 생략) →
  평일 오후 일정 1개 또는 명시적 건너뛰기, 주말 자유 일정 → 20:00 운영관 점호 → 데시멀톡·관계 시간 →
  월간 성장 달력 → 다음 날 08:00과 조간신문`이다. 평일 오후 일정은 운영관 국가망 안내 뒤
  카지노·경마 2개로 시작하고, 예금·부동산 해금 뒤 최대 4개가 된다. 이용하지 않는 날은
  `오늘은 그냥 넘어가기`로 벌점 없이 20:00 점호로 간다. 센터 밖 외출은 호감도 20 이상인
  토·일요일만 허용하고, 빠르게 진행은 최대 90일이며 1년 진행 항목을 두지 않는다.
- 데시멀 투자 결과, 7일 단리 12% 양방향 대여, 자동 상환은 `COHORT_DAILY_INVESTMENT.md`를,
  데시멀톡은 `PHONE_MESSENGER_SYSTEM.md`를 따른다.
- 데시멀톡은 성격·관계·당일/누적 손익·의도·장기기억을 로컬 조합기가 먼저 판정하고,
  서버의 Gemini 3.5 Flash-Lite가 표시 문장만 자연스럽게 다듬는다. 원격 호출이 실패하면
  로컬 조합 답안을 그대로 사용한다. 의미 있는 여자 동기 톡은 모두 관계에 반영하되
  친구별 하루 3회로 제한하고, 같은 의도 반복·경계 침해·검증된 사실과의 모순은 관계를 낮출 수 있다.
- 데시멀톡 대화 1회는 게임 시간 30분을 사용한다. 휴대폰 상단 시계는 저장된 게임 시각을
  표시하고, 마지막 전송 시작은 21:30이며 22:00부터는 모두 취침해 채팅방을 읽기만 할 수 있다.
- 카지노와 경마는 2000년 첫 주식 실습 뒤 한서윤 운영관의 사전 설명을 완료하면
  작업실 PC에서만 접속하는 국가망 확률시장이다. 실제 현장 이동·셔틀·외부 결제는 없다.
  카지노 새 판은 평일 15:00~19:30에만 시작하고 한 판마다 30분을 사용하며 하루 최대
  10판으로 제한한다. 확정 이익의 20%는 국가가 환수하고 반환 원금에는 부과하지 않는다.
  상세 규칙은 `CASINO_SYSTEM.md`를 따른다.
- 생활비 통장은 쓰지 않는다. 국가계좌 주문 가능금을 500원 단위 영속 `chipBalance`로
  바꿔 베팅·지급에 사용하고, 접속 종료 때 남은 칩의 전액 국가계좌 환전 또는 다음 접속용
  보관을 사용자가 고른다. 보관 칩은 저장·복원과 날짜 진행 뒤에도 유지한다.
- 카지노에서 카드 배분·회수·칩 정리 손이 보일 때는 동일한 성인 여성 딜러의 단정한
  손과 흰 커프스·검정 조끼 소매를 사용한다. 결과 공개 전에 손·카드·칩 동작을 재생하되
  모션 감소 설정에서는 즉시 정산하며 애니메이션이 결정론 결과를 바꾸지 않는다.
- 주식·부동산·동네 사업을 연달아 열 때 각 화면이 반환한 최신
  `GameState`를 다음 화면에 넘긴다.
- 주식 기능 전용 `/play/stock-test.html`은 독립 테스트 상태만 사용하며 본편 5슬롯을
  읽거나 쓰지 않는다.

## 편집, 빌드, 검증

- 대사 원본은 `flutter_app/assets/dialogue/dialogue-editor-override.json`이다.
  `/editor` 저장·빌드는 이 파일과 `public/play/`를 함께 갱신해야 한다.
- 장면의 배경·화자·포즈·대사·순서 변경이 Flutter 런타임까지 반영되어야 한다.
  안전 상한은 320장면이다.
- 소스 이미지와 문서를 수정하고 `public/play/`만 손으로 고치지 않는다.
- 기능 변경 후 관련 Flutter 테스트를 먼저 실행하고, 릴리스 전에는 다음을 확인한다.

```powershell
Push-Location flutter_app
flutter analyze
Get-ChildItem test\*_test.dart | ForEach-Object { flutter test $_.FullName }
Pop-Location
npm run lint
npm test
npm run build:release
```

- 전체 Flutter 테스트는 메모리 사용량 때문에 파일별 실행을 기본으로 한다.
- 미완성 기능을 완료로 기록하지 않는다. 현재 남은 기능은 `GAMEPLAY_GAPS.md`에만
  적고, 임시 후보·스크린샷·생성 프롬프트를 루트 정본 문서에 섞지 않는다.

## 파일 관리

- 정본 이미지는 `art_references/`의 승인 앵커와 `flutter_app/assets/images/`의
  런타임 자산뿐이다.
- 이미지 후보, 폐기본, 비교용 실사, 중간 생성물은 저장소에 다시 쌓지 않는다.
  검토가 필요하면 Git 밖의 임시 작업 폴더를 사용하고 승인본만 가져온다.
- `.next/`, `.vinext/`, `dist/`, `work/`, `flutter_app/build/`는 생성물이며 문서나
  코드의 근거로 사용하지 않는다.
- 삭제 전 코드·문서·테스트에서 실제 참조되는지 확인한다. Git 추적 파일 삭제는
  복구 가능한 `git rm` 또는 패치로 수행한다.

## Git 커밋과 푸시

- 사용자가 단순히 `push`, `git push`, `지금까지 한 것 푸시`를 요청하면 저장소 폴더의
  기존 Git 원격과 인증을 사용한다. 먼저 `git remote -v`, `git branch --show-current`,
  `git status -sb`로 대상만 확인하고 GitHub CLI 설치를 요구하지 않는다.
- `gh`는 Pull Request 생성·GitHub 이슈·Actions 조회처럼 GitHub API 기능이 필요할 때만
  사용한다. 기존 브랜치를 커밋하고 원격에 올리는 일은 `git`만으로 처리한다.
- 작업 트리에 여러 작업의 변경이 섞여 있으면 포함 범위를 확인한다. 사용자가 다른
  작업까지 전부 포함한다고 명시했을 때만 `git add -A`를 사용하고, 그렇지 않으면
  요청 범위의 경로만 명시적으로 스테이징한다.
- 이 저장소의 활성 개발·푸시 브랜치는 `main` 하나만 사용한다. 사용자가 명시적으로
  요구하지 않는 한 `agent/*`, `feature/*` 같은 작업 브랜치를 새로 만들지 않는다.
- 기본 순서는 `git switch main` → `git pull --ff-only origin main` → `git add` →
  `git commit -m "<요약>"` → `git push origin main`이다. 기존 작업 브랜치에 커밋이
  남아 있으면 먼저 `main`에 안전하게 병합하고 원격 `main` 반영을 확인한 뒤 삭제한다.
- 푸시 뒤 `git rev-parse HEAD`와 `git rev-parse origin/main`이 같은지,
  `git status -sb`에 미커밋 변경이 없는지 확인한다. 다른 작업이 동시에 새 변경을
  만들었다면 그 변경을 임의로 버리지 말고 별도 후속 커밋으로 보존해 다시 푸시한다.
- API 키·토큰·개인 비밀값은 커밋하지 않는다. 특히 Gemini 키는 서버 비밀 환경변수나
  기기 보안 저장소에만 두고 Git 추적 파일에 원문을 넣지 않는다.
