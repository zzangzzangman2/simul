# 새벽 신문배달 미니게임

최종 갱신: 2026-08-05

## 목적

새벽 신문배달은 주말 행동력 1칸을 실제 플레이와 수입으로 바꾸는 조작형 알바다. 자동으로
결과를 뽑는 카드가 아니라, 플레이어의 주행·투척 성과가 수당과 장부에 그대로 반영된다.

## 한 판의 흐름

1. 2000년 1월 겨울 새벽 주택가 노선에서 자전거가 자동 전진한다.
2. 도로 가로폭을 10개 조향 위치로 나눈다. 도로를 누르거나 손가락으로 드래그하면 가장
   가까운 위치를 즉시 따라가고, 좌우 버튼은 한 번에 한 위치만 미세 이동한다.
3. 왼쪽·오른쪽에 빛나는 우편함이 내려오면 10개 위치를 세밀하게 옮겨 해당 가장자리로 접근한다.
4. 우편함이 노란 타이밍 선에 닿을 때 중앙 신문 패드를 그 방향으로 직접 플릭한다.
5. 방향, 플릭 세기, 타이밍, 차선 거리를 합산해 `PERFECT / GREAT / GOOD / MISS`를 판정한다.
6. 조간 7부를 처리하면 배달률·평균 정확도·최고 콤보·충돌 횟수로 0~100점을 만든다.

충돌은 판 전체를 실패시키지 않는다. `1.4초 손실 + 콤보 초기화`로 처리해 재도전 피로는
낮추되 높은 점수에는 분명한 손해가 생긴다. 장애물도 같은 10개 위치에 생성되고 실제 너비를
고려해 인접 한 칸까지 충돌 범위로 계산한다. 햅틱은 출발·조향 이동·투척·충돌·완주에 서로
다른 강도로 사용한다.

## 점수와 수당

```text
최종 점수 = 배달률 50점
          + 성공한 투척의 평균 정확도 30점
          + 최고 연속 배달 15점
          + 무사고 보너스 최대 5점
```

- 기본 수당은 `900 + 최종 점수 × 16원`이며 2000년 기준 900~2,500원이다.
- 기존 시작 성향·`work_rhythm` 기술·연도 배율은 다른 근로 수입과 같은 방식으로 적용한다.
- 재기 상태에서는 수당을 생활 현금에 기록한 뒤 같은 금액을 실전 증권계좌로 즉시 이체한다.
- 미니게임 결과 없이 `newspaper_delivery` 정산만 호출하면 엔진이 거절한다.
- 완료 결과는 `weekendActivityLog`, 생활 달력, 복식부기 원장에 단 한 번 남는다.

## 렌더링 규칙

- 배경은 `SIMUL polished soft-render VN anime v3`로 생성한 9:16 겨울 새벽 서울 주택가다.
- 플레이어 얼굴은 만들지 않는다. 후면 자전거 배달원은 v3 화풍 앵커로 생성한 투명 ImageGen
  컷아웃을 사용한다. 진행축에 겹치는 앞뒤 바퀴, 프레임·핸들·페달, 머리·목·어깨·관절이
  구분되는 팔과 다리, 겨울 외투·장갑·신발·신문 가방이 작은 화면에서도 읽히며 공식 플레이어
  외형이나 얼굴로 취급하지 않는다.
- 도로 상단의 10칸 조향 레일에서 현재 위치를 항상 표시하며 82ms 보간으로 손가락을 따라간다.
- 물웅덩이·나무 상자·쓰레기봉투도 v3 ImageGen 투명 에셋을 사용한다. 우편함, 신문 투척
  포물선, 눈 입자, 타이밍 선과 동적 그림자만 Flutter 캔버스·위젯 레이어로 그린다.
- 고해상도 배경은 `cacheWidth: 720`으로 디코딩해 모바일 메모리를 제한하고, 입력과 HUD는
  360×800 및 390×844에서 가로 스크롤 없이 동작한다.
- 월드시드와 날짜를 조합한 시드로 장애물 배치를 고정해 저장 재개와 테스트에서 재현한다.

## 주요 구현과 검증

- `flutter_app/lib/rider_mini_game.dart`: 플릭 물리, 10칸 이동·충돌, 이미지·캔버스 합성, 점수 계산
- `flutter_app/lib/game/weekend_activity.dart`: 신문배달 주말 알바와 성과 필드
- `flutter_app/lib/game/game_engine.dart`: 성과형 수당·장부·재기 이체
- `flutter_app/lib/weekend_activity_screen.dart`: 주말 카드에서 미니게임 진입 후 정산
- `flutter_app/test/rider_mini_game_test.dart`: 점수 공식·7부 완주·360px·10칸 조향·ImageGen 에셋·실제 왼쪽 플릭
- `flutter_app/test/weekend_activity_test.dart`: 미플레이 정산 차단·고득점 수당 증가
- `flutter_app/test/weekend_activity_screen_test.dart`: 주말 카드에서 플레이 화면 진입

직접 화면과 플릭 판정만 확인할 때는 `/play/index.html?newspaperPreview=1`을 연다.
이 진입점은 미니게임 화면만 열며 본편 저장 슬롯을 정산하거나 덮어쓰지 않는다.
