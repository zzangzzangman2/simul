# 현재 결정 기록

최종 갱신: 2026-08-02

이 문서는 현재 채택된 결정만 담는다. 날짜별 시행착오와 폐기된 안은 Git 이력에서
확인한다.

## 제품

- 모바일 세로형 게임을 기준으로 한다. 실제 모바일은 뷰포트를 채우고 데스크톱만
  `390×844` 프리뷰를 쓴다.
- 앱은 `처음하기`와 `이어하기`로 시작하고 저장은 최대 5슬롯이다.
- 루트 접속은 `/play/index.html`로 자동 이동한다.
- 기본 UI 글꼴은 Maplestory다.

## 세계관과 서사

- 미래양성원 제6기·국가계좌만 사용한다.
- 주인공은 1987년생 14살, `SEED 01 · 첫빛`, 시작일 `2000-01-02 08:00`이다.
- 국가원금과 주식 실습금은 50,000원이다.
- 정규 프롤로그는 140장면이고 대사 `appearanceVersion`은 13이다.
- 건너뛰기는 청와대·고아원·강당·기숙사를 한 구간씩 넘긴다.
- 보호자·가족·용돈·가정 계좌는 추가하지 않는다.

## 인물과 이미지

- 고정 학생은 플레이어, 김학준, 여학생 8명이다. 담당 교사는 한서윤이다.
- 관계 대상 여학생은 김서아, 이지안, 최이서, 정아린, 박하은, 한수아, 오지우,
  윤채아뿐이다. 범용 여자친구 NPC는 사용하지 않는다.
- 공식 화풍은 `SIMUL production soft-painted VN anime v1` 하나만 사용한다.
- 정본 이미지는 `art_references/` 승인 앵커와 `flutter_app/assets/images/` 런타임
  자산이다. 후보·아카이브·실사 시험본은 보관하지 않는다.
- 여학생 8명은 얼굴과 전신 실루엣이 모두 뚜렷하게 달라야 한다. 공통 교복은
  복부를 가리는 흰 반소매 셔츠, 빨간 체크 넥타이·주름치마, 맨발 산호분홍
  통풍 클로그다.
- 캐릭터 카드는 10명을 보여 주고 초상화를 누르면 상세 프로필로 들어간다.

## 투자와 경제

- 시작 기업 50개는 가상기업이며 회사 정체성은 고정한다.
- 가격·장중 경로·사건·신규상장·분사·폐지는 저장된 월드시드로 결정한다.
- 시장은 09:00 개장, 14:50 종가 동시호가, 15:00 마감이다.
- 현재가와 최우선 호가는 별개다. 10+10 호가와 인라인 주문을 유지한다.
- 호가 미세구조 수치는 `BALANCE_NOTES.md`를 단일 정본으로 둔다.
- 조간신문은 전날 공개 사실만으로 로컬 생성한다.
- 주식·부동산·동네 사업은 같은 월드시드와 공통 경제 사건을 공유하되 미래 정보를
  누설하지 않는다.
- 직접 하루 종료는 15:00 투자 결과, 20:00 관계 시간, 다음 날 08:00과 신문
  순서다.

## 저장과 호환

- 현재 `GameState` 스키마는 v24다.
- 기존 저장의 생성기 버전과 이미 확정된 가격·수익은 보존한다. 신규 자산만 최신
  생성기를 사용한다.
- 월드시드 해시는 `stable_hash.dart`의 VM/Web 공통 31비트 구현을 사용한다.
- 홈 PC의 연속 앱 전환은 각 앱이 반환한 최신 `GameState`를 다음 앱에 넘긴다.

## 제작과 검증

- 대사 정본은 `flutter_app/assets/dialogue/dialogue-editor-override.json`이다.
- 편집기 저장·빌드는 대사 정본과 `public/play/`를 함께 갱신한다.
- `public/play/`는 산출물이므로 직접 수정하지 않는다.
- 완료 여부는 코드와 테스트로 판단하고 미완성 항목은 `GAMEPLAY_GAPS.md`에만 둔다.
- 릴리스 전 `flutter analyze`, Flutter 테스트 파일별 실행, `npm run lint`,
  `npm test`, `npm run build:release`를 확인한다.
