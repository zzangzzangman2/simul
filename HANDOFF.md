# 초딩부터 건물주 인수인계

최종 갱신: 2026-08-01

새 채팅은 이 문서를 먼저 읽는다. 현재 구현은 `WORK_LOG.md`, 남은 범위는
`GAMEPLAY_GAPS.md`, 채택된 규칙은 `DECISIONS.md`가 담당한다. 서로 충돌하면
현재 코드와 `AGENTS.md`를 먼저 확인하고 문서도 같은 변경에서 고친다.

## 1. 지금 어디까지 왔는가

- 기준 브랜치: `main`
- 기준 구현: 현재 `main`의 최신 커밋 (`git log -1 --oneline`으로 확인)
- 주 구현: `flutter_app/`의 Flutter Android·Web 앱
- 웹 진입: `/` → `/play/index.html`
- 저장 스키마: `v24`
- 제품 화면: 모바일 세로 390×844, 최소 너비 360
- 데스크톱: 정확한 390×844 앱 프레임을 가운데 두고, 창이 작을 때만 프레임
  전체를 비례 축소한다. 넓은 대시보드로 바꾸지 않는다.

현재 통합 구현 묶음:

1. 호가를 실제로 소모해야 현재가가 다음 가격으로 갈 수 있는 깊이 제한 가격
   전이. 최우선 호가가 일부라도 남으면 그 다음 호가를 건너뛰지 않는다.
2. 같은 방향·같은 절대가격의 대기잔량만 스냅샷 사이에 승계한다. 매도 주문이
   스프레드를 건넜다는 이유로 같은 가격의 매수 주문으로 바뀌지 않는다.
   호가 행·수량·막대의 UI 키도 방향과 가격을 함께 사용해 반대 방향의 애니메이션
   상태나 가짜 증감값을 빌리지 않는다.
3. `잼민 라이더`의 주인공 72×128 투명 픽셀 스프라이트, 픽셀 도로·장애물·
   체크포인트와 짧은 바운스·차선 기울기 연출.
4. 데스크톱 브라우저의 정확한 390×844 휴대폰 미리보기와 작은 창 비례 축소.
5. 위 변경을 포함한 Flutter Web release의 `public/play/` 동기화.
6. 튜토리얼·이름 입력 없이 바로 여는 `/play/stock-test.html` 주식 전용
   테스트 진입점. 09:00·100만 원·최고 주문 권한으로 시작하고 실제 5슬롯
   저장을 읽거나 쓰지 않으며 새로고침하면 테스트 상태만 초기화한다.
7. 현행 v24 저장의 `BusinessPortfolioState`, 투자실 PC `동네상권넷`, 18업종·
   6개 미시 입지 `locationId`와 전국 32개 실제 상권 `districtId`, 2000~2026
   7국면·49개 상권 연표 사건, 6축 운영 정책, 회사 통장 인수·투자·월 정산,
   지역이 일치하는 공실 상가·오피스 직영점, 사건 선택·지연 결과, 미지급금과
   3개월 강제폐업을 잇는 동네 사업 경영.
8. 주식의 기존 실물경제 역사 촉매·코퍼스 0단계 거시 사건을 정규 원본으로
   사용하는 `world_economy.dart` 공통 경제층. 주식은 기존 가격·장중 경로를
   그대로 유지하고, 신규 사업 v3/상권 v2와 신규 부동산 v4만 같은 사건
   ID·발생일·제목·이미 시드가 반영된 영향값을 각 자산 효과로 투영한다.
   장중 공개는 날짜 단위 자산에 다음 달력일부터 전파하고 순수 시장구조 사건은
   제외하며, 기존 사업 v1·v2와 부동산 v1·v2·v3은 동결한다. 부동산 14권역은
   중앙 매핑으로 32개 사업 상권에 연결한다.
9. 일일 예상 거래대금 20억원 이상 종목의 중앙 최우선이 전량 체결되면 소진된
   방향은 같은 가격에 다시 채우지 않는다. 그 절대가격에 반대 방향의 새 일반 큐를
   만들어 경계를 전진시킨다. `13,400원 bid / 13,450원 ask`에서 ask 전량 체결 뒤
   `13,450원 bid / 13,500원 ask`가 회귀 기준이며 모든 양수 인공 큐는 최소 10주다.
   전진한 경계는 최근 합성 체결 하나가 사라져도 종목·거래일 세션에 별도로 남아
   다음 펄스가 이전 중앙가로 되감거나 소진 벽을 부활시키지 않는다.
10. 하단 `매도`·`매수` 도크를 누르면 320ms 동안 주문표가 왼쪽에서 밀려 들어오고,
    기존 10+10 압축 호가는 같은 인스턴스를 유지한 채 오른쪽으로 밀리며 줄어든다.
    전환 전·도중·완료 뒤와 매수↔매도 변경에도 같은 절대가격의 매도벽·매수벽과
    잔량을 보존한다. 중앙 빨간 테두리의 세로 좌표는 체결 가격 변경 때만 움직이며
    주문표를 열거나 닫는 동작만으로 여러 호가를 건너뛰지 않는다.
    호가 펄스는 주문표와 상세 화면 뼈대를 재마운트하지 않고 호가·가능수량·
    미리보기만 국소 갱신하며, 일시정지는 현재 표시 스냅샷과 애니메이션을 동결한다.
11. 소진된 일반 호가의 첫 재유입은 대체로 목표의 28%, 최대 45%로 제한한다.
    여러 0 큐는 선택된 1~3개 가격부터 순서대로 다시 들어오며, 이후 양수 큐도
    선택된 펄스에서만 목표량까지 성장한다. 전량 체결로 관통된 구조벽 가격에는
    예전 벽 수량·표시가 아니라 반대 방향의 작은 일반 큐를 만들고, 구조벽 일반
    회복 상한 안에서 점진적으로 늘려 최소 10주에 고착되지 않게 한다.

12. 신규 사업 v3의 구조적 적자는 12개월 연속 적자와 누적 손실
    `max(2,000만원, 현재 장부가치 50%)`에서 기존 강제청산 워터폴로 닫는다.
    비용을 낼 현금이 많아도 무한 존속하지 않으며 사업 v1·v2는 소급 변경하지 않는다.
13. 사무실 빠른 진행의 `1년 저개입 진행`은 중요뉴스를 날짜별 신문함에 보관한
    채 계속 진행하고, 사용자 안건·캠페인 종료에서만 멈춘다. 날짜 정산과 주간
    체크포인트는 일반 진행과 동일하다.
14. 장부의 `총자산 구성`은 현금·예수금, 정기예금, 원화 주식, 부동산, 사업체와
    총부채를 한 카드에서 비교한다. 360px 두 열 배치와 음수 현금의 부채 합산을
    포함하며 기존 순자산식은 바꾸지 않는다.

15. 매 새 호가 펄스마다 화면 안 10+10의 매도 또는 매수 대표 벽 한 줄이
    결정론적 기관·연기금·미세 수급에 따라 유입되거나 취소된다. 양쪽 방향을
    직전 수정 방향까지 스냅샷에 기억해 배속·누락 프레임에도 펄스마다 번갈아
    선택하고 대표 절대가격을 고정한다. 개별 변화는
    평시 1.2%·급변 2.0% 이하이며 중앙 체결 테두리·체결 테이프와 독립한다.
16. 신규 게임 프롤로그는 1981년 가상 지도자 전두광의 미래양성계획 결재부터
    2000년 새봄보육원 출발, 미래양성원 정문, 제6기 오리엔테이션, 열 명의
    공용 책상과 아래층 침상에 둥글게 앉아 이어 가는 열 명의 첫인사·침상과
    사물함 배정·생활 경계 합의·첫 갈등·제5기 배지 비밀 대화,
    둘째 날 주식 PC 실습실까지 140장면으로 이어진다. 주인공은 1987년생이며 2000년 한국식 나이
    14살, `SEED 01 · 첫빛`으로 시작해 19살 `SEED 06 · 내 이름`에서 수료한다.
17. 모든 저장은 미래양성기금 명의 국가원금, 확정 순이익 20% 국가 환수,
    80% 자립적립금 규칙을 사용한다. PC에서 이름을 확정하면 첫 저장을 만든다.
18. 신규 정식 화풍은 `SIMUL production soft-painted VN anime v1`이다. 실사 전환은
    중단했으며 실사 인물·배경·후보·참조 사진은 삭제했다. 제6기 여학생 8명은
    `flutter_app/assets/images/production_soft_painted/`에 정식 연결되어 있다.
    김서아·이지안·최이서·정아린·박하은·오지우·윤채아는 각 9종이다. 한수아는 v1·v2 합계 16종을 보관하고 런타임은 v2 표정·동작 8종을 사용한다.
    전원 공통 규정 하복과 맨발 코랄핑크 클로그를 사용하되 얼굴·키·체형·소품은
    캐릭터별 승인 정체성을 유지한다. 후보 폴더를 승인 없이 런타임에 연결하지 않는다.
19. `/editor`의 `appearanceVersion: 13` 정규본은 140장면이다. 기존 장면의 배경·장 메타데이터·화자·화자별 포즈·지문·대사를
    수정하고, 완성형 작성 창으로 새 장면을 선택 장면 다음에 추가할 수 있다.
    저장·빌드는 대사 JSON, Flutter Web release와 `public/play/`를 함께 갱신하며
    런타임은 최대 240장면 안에서 편집본의 실제 마지막 장면까지 재생한다.
20. 직접 `하루 보내기`를 누르면 15:00에 제6기 10명의 투자 결과표를 확인한 뒤 20:00에 여학생 8명 중 한 명과 대화·데이트하거나 혼자 쉰다. 관계 선택은 하루 한 번이며 전원 1점에서 시작하고, 호감도 20부터 공개 장소의 또래 데이트가 열린다. 결과 저장 뒤 다음 날 08:00과 조간신문으로 이동한다.
21. `뚜둥! 오늘의 투자 결과`는 플레이어·김학준·여학생 8명의 순위, 오늘 투자 종목, 손익, 총금액을 10행으로 보여 준다. 플레이어는 실제 국가계좌 종가 평가액을 쓰고 나머지 9명은 결정론적 가상기업 투자 수익을 쓴다. 나보다 총금액이 적은 동기에게 하루 한 번, 두 사람의 총액 차이와 출금 가능 현금 이내에서 7일 무이자로 빌려줄 수 있다. 대여채권은 자산이며 자동 일부·전액 상환하고 호감도는 바꾸지 않는다.
22. 생활 허브 오른쪽 휴대폰 아이콘은 `미래톡`을 열고 읽지 않음 9개로 시작한다. 김학준과 여학생 8명의 1:1 채팅, 추천 답장, 최대 80자 자유 입력, 친구별 하루 3회, 최근 256개 저장을 지원한다. 여학생 8명은 같은 질문에도 각 MBTI 문서의 판단축·말풍선 리듬·갈등 반응으로 서로 다르게 답하며 메시지 자체는 호감도를 올리지 않는다.
## 2. 신규 게임의 정확한 순서

다른 문서에 아래와 다른 과거 순서가 보이면 이 순서로 고친다.

1. 타이틀에서 `처음하기`를 누르면 프롤로그보다 먼저 월드시드를 확정한다.
2. `세계관 → 2000~2026 주식시장 → 서울·경기 부동산 → 공통 경제 연결 → 현재 날짜 비누설 검증` 순서로 세계를 예열한다.
3. 1981년 청와대 심야회의에서 전두광과 정책실 인물들이 미래양성계획의 원금·환수·자립적립 조건을 결재한다.
4. 1982년 국립 미래양성원 개원 기록을 본다.
5. 2000년 1월 2일 14살 주인공이 새봄보육원을 떠나 미래양성원 정문에서 수아와 학준을 만난다.
6. 한서윤의 제6기 오리엔테이션 뒤 첫날 남녀 공동 기숙사와 생활 공간을 안내받는다.
7. **PC 실습 연결:** 둘째 날 주식 PC 실습실에서 06번 CRT가 꺼진 상태로 시작하며 전원을 직접 켜거나 끄고 바탕화면의 `주식실습` 앱을 연다.
8. **국가계좌 첫 저장:** 운용자 이름과 투자장부 이름을 입력하면 분석·손실 기록 원칙으로 같은 시드의 첫 저장과 미래양성기금 10,000원 국가계좌를 만든다.
9. **첫 주문 연결:** 한서윤의 강제 실습에서 홈·주식·내 투자 구조, 검색·분류, 한빛통신 현재가, 차트, 정보·투자노트, 호가, 주문 탭의 매수·매도·정정/취소·미체결·잔고, 잔액 부족·부분체결 실패, 지정가 1주 매수, 5분 단위 가격 변화, 매도와 확정손익을 익힌다. 수아와 김학준이 오해·반론을 주고받고, 거래대금·회전율·VI·거래정지·동시호가는 첫 언급 즉시 생활어로 설명한다. 매수 이유와 매도 조건을 실제 투자노트에 모두 저장해야 주문으로 넘어가며, 거래 뒤에는 세 선택지 중 하나를 골라 분기 피드백을 받는다. 한서윤은 승인된 6포즈를 수업 단계에 맞춰 사용한다.
10. **실습계좌:** 실제 시장 시세·호가와 주문 엔진을 쓰되 수업용 10,000원 계좌에서 거래하여 완료 뒤 정식 국가계좌 원금과 포지션은 보존한다.
11. **생활 허브:** 기숙사·공용 생활실·식당·중앙 복도·본관 앞의 미래양성원 논리 장소를 사용한다. 최종 배경 에셋 연결은 남은 작업이다.

캠페인 저장 날짜는 실습 중에도 2000-01-02로 유지한다. 실습만 다음 거래일의
연습 시장과 별도 연습 계좌를 사용하며 본 저장의 현금·포지션·원장을 바꾸지
않는다. 이어하기는 저장된 시드의 세계 예열에 성공한 뒤에만 활성 슬롯을 바꾼다.

리부트 신규 플레이의 경제·성장 상태:

- 주인공: 1987년생, 2000년 한국식 나이 14살
- 과정: 미래양성원 제6기, `SEED 01 · 첫빛`
- 국가원금: 미래양성기금 명의 10,000원
- 확정 순이익: 국가 환수금 20%, 자립적립금 80%
- 회사 통장: 국가계좌·자립적립금과 별도
- 기존 저장: 날짜·현금·시장 자산은 보존하고 조직·시설·서사는 미래양성원 기본값으로 정규화

## 3. 현재 구현된 큰 범위

### 시작·생활·저장

- 최대 5개 저장 슬롯, 자동·수동 저장, 정상 백업과 손상 원문 보존
- 1981년 정책실 → 1982년 개원 → 2000년 보육원 출발·정문·오리엔테이션 → 첫날 기숙사 → 둘째 날 주식 PC 실습실 소개의 리부트 프롤로그
- 1987년생·한국식 나이·제6기·SEED 01~06·국가계좌를 저장하는 리부트 `StoryState`
- 한서윤의 별도 연습 계좌 매수·가격 변화·매도·확정손익 복기
- 미래양성원 기숙사·공용 생활실·식당·중앙 복도·본관 앞 허브와 투자실 PC, 장부, 조직, 안건, 일거리
- 홈 PC의 중첩 앱은 직전 앱이 반환한 최신 `GameState`를 다음 앱에 전달
- 여학생 8명 호감도 1~100, 관계 단계, 일상 대화·20점 데이트 해금, 하루 중복 방지, 휴식, 관계 목록과 최근 선택 기록. 지안·이서·지우의 정식 전신 연결은 아직 남아 있음
- 제6기 10명 15:00 일일 투자 결과표, 실제 플레이어 종가 평가, 9명 결정론적 NPC 투자, 하루 1회 동기 대여, 7일 무이자 자동 상환과 대여채권 자산 합산
- 미래톡 휴대폰 표시, 9명 연락처·읽지 않음 배지·자유 입력과 추천 답장·친구별 하루 3회·MBTI별 주식/수업/일상/경계 반응 저장
- 현실 조작형 일거리 `잼민 라이더`는 교사 감독 원내 폐쇄 코스로 연결

### 주식시장

- 고정 출발 가상기업 50개와 시드별 IPO·분사·유상증자·상장폐지
- 기준 시드에서 212개 종목, 단계형 사건 38,970개, 2026-12-31 최종 결산
- 09:00 개장, 14:50~15:00 종가 동시호가, 15:00 마감
- 2015-06-14까지 ±15%, 이후 ±30% 가격제한폭
- 종목 상세의 `호가 · 주문 · 차트 · 정보` 고정 탭
- 매도 10행·매수 10행 고정 대칭 호가, 왼쪽 압축 주문표와 오른쪽 호가를 함께
  유지하는 인라인 주문
- 실제 잔량·분당 용량을 공유하는 시장가 IOC·지정가 부분체결·미체결 대기열
- 누적 거래량·거래대금·체결강도·잔량비, 최근 체결 테이프, 상태 배지, 고정 주문 도크
- 게임분당 결정론적 호가 슬롯: 거래대금 기본 1·1·1·1·2·2·3·4·4회,
  급변 5회, 극단 7회. 20~75억원은 1회/분이며 75억원 미만은 단방향
  소량 체결강도만으로 가속하지 않음. 75억원 이상도 최근 3시장분의
  고유 `(시장분, 미세구조 프레임)` 표본 최소 3개와 체결대금 합계 0.10억원 이상이 있어야 불균형 가속. 실제 벽 잔량과 `0.45` 두께는 유지하고 축소된 슬롯에 누적 체결 용량을 비례 배정해 마지막 슬롯에서 같은 목표가에 도달
- 심리 가격과 5·20·60주 이동평균을 합친 구조 유동성, 벽 흡수·붕괴·공백
- 실제 분봉 거래량 상한, 플레이어 대량 체결의 제한된 6틱·6분 시장충격
- 미래종합·도전종합과 상위 3개 업종 지수
- 매도대금 T+2 출금 제한, VI·중요공시 거래정지·관리주의
- 합병·포괄적 주식교환 원가 승계, 공개매수 정산, 유상증자 선택과 공시 일정

### 공통 세계경제

- 정규 원본은 `market_era_events.dart`에서 주식 가격·뉴스가 이미 쓰는 시드형
  실물경제 역사 촉매와 코퍼스 0단계 거시 사건이다. 별도 난수로 영향 강도를
  다시 뽑지 않고 같은 ID·발생일·제목·시드 반영 영향값을 보존한다.
- 플래시 유동성 사고, 공매도 규칙 변경, 레버리지 청산 같은 순수 주식시장 구조
  사건은 제외한다. 기업·기술·후속 코퍼스 단계도 주식 전용이다.
- 주식 가격과 장중 경로는 이미 이 원본 사건을 반영하고 있으므로 변경하지 않는다.
  추가 거시 배율을 주식에 곱하거나 기존 저장을 재가격하지 않는다.
- `worldEconomySnapshot`은 같은 원인을 사업의 수요·임대료·경쟁·임금·공실·
  위험·활력과 부동산의 가격·임대료·공실·위험·수리비·유동성으로 각각 투영한다.
- 주식에서 08:00 뒤 장중 공개되는 사건은 날짜만 아는 사업·부동산 화면에는 다음
  달력일부터 보인다. 현재 날짜 이후 사건은 어느 자산 화면에도 노출하지 않는다.
- 공개 목록은 최근 12건·최근 730일을 기본 경계로 쓰되, 숫자 영향이 아직
  활성인 사건은 목록 한도를 넘어도 남겨 현재 배율의 원인을 조사할 수 있게 한다.
- 이 층은 저장 상태가 아니라 월드시드·현재 날짜에서 계산하는 파생 세계 뷰다. 현재 `GameState` 스키마는 v24이며 신규 사업 v3/
  상권 v2와 신규 부동산 v4만 사용한다. 기존 사업 v1·v2와 부동산 v1·v2·v3은
  각 버전 공식을 그대로 재생한다.
- FNV 계열 결정론 해시는 `stable_hash.dart`의 31비트 정확 곱셈을 공유해
  Flutter VM과 Web JavaScript에서 같은 월드시드 결과를 낸다.

### 부동산·은행

- 서울·경기 18개 기준 자산, 54개 매물, 14개 권역. 14개 권역은 한 중앙
  매핑으로 32개 사업 상권 중 대응 상권에 연결된다.
- 저장된 부동산 v1·v2·v3 생성기 보존과 신규 매입 v4 고정
- v4는 지역 사건 중 금리정책·주택정책·임대차정책의 숫자 영향만 공통 경제로
  교체한다. 지역 `commercialCycle`·`demographicShift`와 교통·재개발·건물
  사건은 계속 수치에 반영하고 기존 15개 인과군·15,000개 이상 문장 조합을 유지한다.
- 공통 유동성은 NPC 매물 체류기간을 `listedAt`, 보유자 매각 대기일을
  `saleListedDay` 시점에 각각 한 번 고정한다. 매수자 제안 준비일은 저장된
  `saleOfferIssuedDay`로 유지해 공식 변경 뒤에도 다시 계산하지 않으며
  v1·v2·v3 기간에는 손대지 않는다.
- 지역뉴스 탭의 v4 공통 경제 구역은 주식·상권과 같은 사건 ID·발생일·제목을 보여 준다.
- 공실·월세·전세, 담보대출·DSR·총부채, 세금·보험·수리·강제매각
- 총자산에서 모든 대출·보증금 반환채무·결손·미납비를 빼는 단일 순자산식
- 서하늘 공인중개사 6상태 안내
- 정기예금, 신용대출, 신용점수와 저장 가능한 월 정산

### 동네 사업 경영

- 작은방 홈 PC의 `동네상권넷`에서 `인수·창업 / 내 점포 / 사건함 / 월별 손익 /
  상권판세` 5탭을 연다. 홈 PC는 주식·부동산·동네상권넷·별빛 상점의 2×2
  그리드다.
- PC방·노래방을 포함한 18업종을 제공한다. 대학가·오피스 상권·주거 밀집지·
  역세권·전통시장·번화가는 점포 주변 성격인 6개 `locationId`이고, 강남·홍대·
  성수·판교·송도·서면·해운대·대구·대전·광주·울산·세종·제주 등을 포함한
  전국 32곳은 실제 지리 단위인 별도 `districtId`다. 둘을 같은 “입지”로
  합치지 않는다.
- 32개 상권은 2000~2026에 태동·급성장·성숙·과열 진정·쇠퇴·위기·재생의
  7국면을 오간다. 공통·주요 사건 17개와 지역 전환 사건 32개, 총 49개 **지역
  상권 연표 사건**은 그대로다. 수요·임대료·경쟁·임금·공실·위험과 업종
  적합도가 지역마다 다르다.
- 신규 사업 v3가 사용하는 상권 생성기 v2는 지역 연표의 구형 전국 거시 사건을
  숫자 계산에서 제외하고 공통 경제 정규 사건을 한 번만 투영한다. 같은 사건
  ID는 `공통경제 · ...` 공개 이력으로 상권판세에서 확인할 수 있다.
- `상권판세`는 현재 순위·전월/전년 추세·지표·현재 신호와 현재 날짜까지 공개된
  원인만 표시하고 미래 사건을 누설하지 않는다.
- 매물 가격·보증금·예상매출은 월 1일 상권 스냅샷으로 고정해 같은 달에는 같은
  견적을 유지한다. 실제 영업 손익·임금·위험·사건은 정확한 게임 날짜의 상권
  상태를 사용하고, 임차료는 개점 당시 대비 현재 시대 비용지수와 상권 임대료
  지수 비율을 함께 반영한다.
- 신규 매물은 포트폴리오의 레거시 버전과 무관하게 현재 사업 생성기 v3·상권
  생성기 v2로 만들고, 이미 보유한 점포만 저장된 버전을 동결한다. 기존 점포
  인수는 매물의 원래 `locationId`를 강제하며 신규 창업만 미시 입지를 선택한다.
- 가격·품질·인력·홍보·영업시간·설비관리의 6축 정책과 설비·리모델링·교육·
  홍보·안전·수용력 추가 투자가 매출과 비용·사건 위험을 함께 바꾼다.
- 모든 인수·창업·추가 투자·사건 대응·월 정산은 회사 통장만 사용한다.
  사업 장부가치는 총자산, 미지급금은 총부채와 순자산 계산에 반영된다.
- 보유 공실 상가·오피스 빌딩은 중앙 매핑에서 정규 지역을 확인할 수 있고 실제
  `districtId`가 매물과 같을 때만 직영점에 연결한다. 지역 정보가 없는 구형
  부동산은 거절한다. 직영 창업은 부동산 지역으로 잠기고 사업 임대료만 0이 되며
  부동산 운영비는 남는다. 연결 중 임대·매각·리모델링을 막고 정리 뒤 공실로 되돌린다.
  깨진 연결은 같은 상권의 안전 임차로 바꾼다.
- 첫 완전 영업월부터 매출과 8개 비용 항목을 월 1회 정산한다. 현금 부족분은
  미지급금이며 3개월 연속 남으면 강제폐업·잔존자산 상계로 이어진다.
- 정리·강제폐업의 실현손익은 `총 회수액 - 사업 장부가치`다. 미지급금 상환 뒤
  남은 순현금으로 다시 계산하지 않아 부채 상환을 처분손실로 중복 반영하지 않는다.
- 자산 상계 뒤 남은 미지급금은 청산 직후 해당 점포부터 회사 통장으로 갚고,
  하루의 예금·경제·이벤트 유입이 끝난 뒤 폐업·매각 점포 ID 순서로 다시 갚는다.
  증권 예수금은 사용하지 않으며 부족분은 보존하고 두 단계 처리 ID를 분리한다.
- 점포 운영 사건은 공통 템플릿 25종 이상, PC방·노래방 전용 템플릿 각각 8종
  이상이다. 이는 49개 지역 상권 연표나 공통 경제 정규 사건과 다른 선택형 운영
  사건이다. 선택지와 기한을 저장하고 결과는 예정일에 같은 시드로 성공·부분
  성공·실패 중 결정한다.
- 사업 데이터가 없는 저장과 빈 포트폴리오는 현재 초기 버전 v3으로 복원한다.
  신규 점포는 사업 생성기 v3·상권 생성기 v2와 `districtId`·개점 기준
  임대료지수를 저장한다. 기존 사업 생성기 v1·v2는 공통 경제를 소급 적용하지
  않고 각 수치 공식을 보존하며, 지역 필드가 없는 v1은 기존 `locationId`와
  중립 상권 계산을 유지한다.

## 4. 호가 엔진에서 혼동하면 안 되는 현재 규칙

- 일반주권 20,000원 이상 50,000원 미만의 KRX 가격단위는 유가증권시장·
  코스닥시장 모두 50원이다. 32,150원 다음 유효가격은 32,200원이다.
- KRX 연속매매는 최저 ask·최고 bid를 가격 우선으로 대조하고 같은 가격에서는
  시간 우선을 적용한다. 현재가·최근 체결가는 직전 실제 체결가격이고,
  최우선호가는 남은 주문의 최저 매도·최고 매수이므로 별도 상태로 유지한다.
- 구조벽 후보와 벽 메타데이터는 월드시드·종목·절대가격에 붙는다.
- 실제 standing order 수량은 `절대가격 + 방향(ask/bid)`에 붙는다.
- 화면 행이 바뀌어도 같은 방향·같은 가격의 큐는 완만하게 승계한다.
- 가격이 스프레드를 건너면 남은 ask를 bid로, bid를 ask로 변환하지 않는다.
- 엔진은 완전 소진 가격과 원래 깊이의 회복 목표를 내부에 기억하지만 운영 10+10에는 숫자 0 행을 표시하지 않는다. 일반 큐는 최우선에서 평온·급변 모두 약 28%로 시작해 세 논리 슬롯 안에 목표의 70% 이상을 회복하고, 거래대금·급등락·기관·연기금 수급을 반영한다. 급변장은 더 많은 슬롯과 이동 강도 가중으로 유입이 커지고, 구조벽과 산발적 미시 벽은 거래대금 기반 trickle만 받는다. 재유입과 정상 변동은 `1 / (1 + 거리×0.55)`로 감쇠해 10호가는 최우선보다 약 6배 느리다.
- 일일 예상 거래대금 20억원 미만 초저유동 종목만 결정론적 내부 빈 가격과 낮은 확률의 최우선 공백을 허용한다. 20억원 이상 생성형 운영 사다리는 바깥 호가와 구조 공백까지 최소 10주의 얇은 일반 큐로 채워 모든 유효 틱을 잇는다. 20억원 이상 중앙 최우선의 자동 전량 체결은 소진된 같은 방향 큐를 다시 채우지 않고 같은 절대가격에 반대 방향의 새 일반 큐를 만든다. 따라서 `13,400원 bid / 13,450원 ask`에서 ask가 전량 체결되면 `13,450원 bid / 13,500원 ask`로 전진해야 한다. 13,450원 ask 리셋과 13,550원 ask 건너뛰기는 모두 실패다. 32,500원 ask까지 실제로 연속 소진했다면 경계는 32,500원 bid로 남고 다음 ask는 32,550원이어야 하며 새 반대방향 체결 없이 32,150원으로 되감지 않는다. 화면은 0주 대신 더 깊은 양수 가격을 올려 10+10을 유지하고, 20억원 미만 저유동 IOC만 실제 빈 가격을 건너뛸 수 있다.
- 이전 저장·스냅샷이 `32,150원 bid / 32,500원 ask` 레거시 갭이어도
  32,500원 ask 전량 체결 즉시 `32,500원 bid / 32,550원 ask`로
  정규화한다. 실제 새 반대방향 체결 없이는 32,150원으로 되감지 않는다.
- 실제 KRX에는 중간 가격 주문이 없는 호가 공백과 1주 주문이 모두 가능하다.
  위 한 틱 연속성과 최소 10주는 거래대금 20억원 이상 종목에서 시뮬레이션이
  생성한 가시 큐에 적용하는 게임 현실감 정책이며 법규상 최소수량이 아니다.
- 구조벽은 가격 경계 통과 또는 최초·회복 기준 잔량 90% 이상 실제 소진 중 하나로 붕괴한다. 소진 기반 붕괴는 벽 표시를 해제하고 회복 목표를 기존 벽/strength 이하로 낮추며 다음 펄스·분 경계·방향 전환에도 유지한다. 이미 0으로 소진됐거나 방향이 바뀌어 운영 10호가에 새로 들어온 붕괴 구조 큐는 20억원 이상이면 과거 벽 수량·상태 없이 최소 10주의 얇은 일반 큐만 허용하고, 20억원 미만이면 실제 0 공백을 유지할 수 있다. 멀어진 뒤에도 일반 유입의 20%만 허용하며 벽 상태는 부활시키지 않는다. 중앙 최우선 전량 체결 전이도 소진된 구조벽을 같은 방향으로 복원하지 않으며 새 반대편 일반 큐에 과거 벽 수량과 벽 상태를 넘기지 않는다. 아직 양수 잔량이 남은 붕괴 큐는 구조 공백 배율만큼 얇아진다. 생성·승계·전량 체결 전이의 모든 양수 호가는 최소 10주다. 가격 접근으로 벽이 취소·정정되어 줄어든 수량은 체결 토큰·원장 watermark·거래량·체결강도에 넣지 않는다.
- 합성 큐를 실제로 일부 소모한 뒤 1~9주만 남으면 잔여분은 즉시 취소한다.
  이 취소분은 `lastSyntheticTrade`, 체결 테이프·거래량·체결강도·분당 예산·
  가격별 원장 watermark에 넣지 않는다.
- 라이브 UI의 같은 분 carry는 플레이어 체결을 누적 원장 소모·공유 용량과 함께
  현재 스냅샷에 정규화하고 그 결과를 다음 프레임에 넘긴다. 최신 플레이어 체결의
  명시적 방향·가격이 최우선 전량 소진을 증명할 때만 중앙 경계를 승격한다.
  실제 체결 전에는 직전 최우선 bid를 유지하고 결정론적 목표가격만으로 방향을
  뒤집지 않는다. 합성 체결의 잔량·소진 판정은 현재 표시된 실행 스냅샷의 같은
  ask/bid 방향·절대가격 큐만 사용한다. 이전 carry의 더 큰 수량이나 반대 방향
  depth를 대신 써서 현재 줄어든 벽을 되살리지 않는다. 이전 carry의 더 작은 같은
  방향 잔량은 같은 프레임 신규 유입을 막는 상한일 뿐 현재 잔량의 원본이 아니다.
- 현재가가 목표 경로를 향해 움직일 때는 해당 방향의 실제 standing depth를
  최우선부터 소모한다. 최우선이 부분체결이면 현재가는 그 가격까지만 가고 다음
  가격으로 넘어가지 않는다.
- 이 depth walk는 분 내부에서만 한다. 게임분 경계에서는 화면을 열었는지와
  무관하게 결정론적 세션 경로값으로 마감한다.
- standing depth 계수는 `0.45`, 체결능력 계수는 `0.48`이다. 체결강도는
  체결 테이프의 매수/매도 체결량 비율이고, 주문장 총잔량 비율은 `잔량비`다.
- 구조벽은 현재가 아래에서는 지지, 위에서는 저항으로 역할을 바꿀 수 있다.
  이것은 standing order 수량의 방향 전환과 다른 개념이다.
- 같은 분의 일반 펄스는 정상 큐의 선택된 1~3개 가격만 바꾸고 나머지는 승계한다. 소진 뒤 회복 중인 일반 큐도 선택된 가격만 점진 재유입한다. 여러 가격이 동시에 0이면 모든 행을 함께 다시 만들지 않고 선택된 행부터 순서대로 복구하며, 분 경계의 가격 경로는 결정론적으로 마감하되 대기잔량을 전체 템플릿 수량으로 리셋하지 않는다.
- 분 경계에서 여러 가격을 한 번에 통과하더라도 `orderedFills`·`syntheticTradePrints`·`sweepSteps`가 중간 호가별 실제 소진 수량과 남은 수량을 순서대로 보존한다. 일반 부분체결은 같은 standing side와 남은 수량을 유지해 다음 가격을 건너뛰지 않는다. canonical 가격·수량은 즉시 최종 상태로 확정하되, UI는 다중 단계 또는 단일 호가의 실제 경계 통과를 단계당 56~96ms·총 약 480ms로 재생한다. 상세 호가의 소진 막대는 실제 잔량 열에만 그리고 진행 문구는 반대편 비어 있는 흰 124px 셀에만 표시해 중앙 가격과 빨간 체결 테두리를 덮지 않는다. 압축 호가는 행 위 문구를 없애고 헤더의 잔량 자리에 진행 상태만 잠깐 표시한다. 기존 144ms 보간은 바꾸지 않는다. 3배·10배속에서도 미재생 시장·플레이어 배치를 합치거나 버리지 않고 종목·거래일 세션 캐시 소유 FIFO journal에 생성 순서로 보존해 모든 벽이 `arriving → draining`을 거친다. 차트·정보 탭과 상세 화면·경로 재진입 뒤에도 미재생 배치를 보존하고 `stepIndex/arrived` 체크포인트에서 이어 재생해 체결 전 벽을 되살리지 않는다. 90% 이상 소진된 구조벽의 붕괴 상태와 낮아진 회복 상한도 다음 분 carry에 그대로 승계한다.
- sweep 소진 막대는 실제 깊이와 무관한 `begin=1.0` 풀폭을 금지한다. 기존 호가와 같은 안정 depth scale을 분모로 `(consumedQuantity + remainingQuantity) / stableMaxDepth → remainingQuantity / stableMaxDepth`만 재생하므로, 124px 전체 폭은 체결 전 실제 잔량이 그 scale 최댓값 이상일 때만 나온다. 단계 위치는 행 사이를 보간하지 않는 정확한 `Positioned`로 해당 가격에 즉시 붙이고, 압축 호가는 가격과 ask/bid 방향이 모두 같은 행에만 막대를 표시한다.
- 엔진의 canonical 정산은 연출을 기다리지 않고 즉시 끝낸다. UI는 재생 중인 각 `snapshot + sweepSteps` 배치에서 아직 차례가 오지 않은 미래 호가의 방향·가격·체결 전 잔량을 고정하고, 실제 체결 step마다 가운데 빨간 테두리를 현재 대상 호가에 먼저 도착시킨 뒤 그 호가 잔량을 소진한다. 이 순서는 ask/bid 양쪽의 부분체결·단일 호가 체결·다중 호가 체결과 플레이어 즉시·대기주문 체결에 모두 적용하며 새 canonical 스냅샷이 재생 중 배치의 미래 벽을 미리 줄이거나 옮기지 않는다. 같은 프레임·같은 가격의 대기주문 체결도 남은 플레이어 수량을 역누적해 중간 벽이 되튀지 않는다. 비체결 취소·정정 감소는 수량 숫자에만 조용히 반영하고 `취소 N` 플래시·감소 꼬리표·체결 테두리·체결 테이프를 만들지 않는다. 취소-only 패킷은 FIFO 연출에 넣지 않으며, 실제 체결 가격만 이전 스냅샷에서 복원해 테두리·테이프와 동기화한다.
- 체결 테이프도 호가 테두리와 같은 FIFO 표현 시계를 사용한다. `arriving` 중에는 아직 도착하지 않은 미래 체결을 먼저 노출하지 않고, 테두리가 정확한 가격에 도착해 `draining`으로 바뀌는 프레임에만 같은 방향·가격의 활성 행과 체결 테이프를 함께 표시한다.
- 거래대금 기본 펄스 구간은 게임분당 1·1·1·1·2·2·3·4·4회이고,
  급변 5회·극단 7회다. 직전 슬롯 수의 약 1/3이며 20~75억원 구간은
  1회/분을 유지한다. standing depth 계수 `0.45`와 실제 벽 잔량은 줄이지 않고, 축소된 슬롯에 누적 체결 용량을 비례 배정해 마지막 슬롯에서 같은 목표가에 도달한다. 75억원 미만 종목은 체결강도 소표본이
  한쪽뿐이라는 이유만으로 급변·극단에 승격하지 않는다. 75억원 이상도 최근
  3시장분에 고유 `(시장분, 미세구조 프레임)` 표본 최소 3개와 실제 체결대금 합계 0.10억원 이상이 없으면 체결강도
  불균형 가속을 허용하지 않고 기본 슬롯을 유지한다. 실제 가격 이동 증거는 별도다.
- 표시 스냅샷과 원장 watermark가 다르면 즉시 주문을 거절한다.
- 일시정지·오버레이·휴장·장마감·차트·정보 탭에서는 호가 펄스를 멈춘다.
- canonical 가격·잔량은 상태 변경 즉시 확정한다. 화면의 비체결 일반 변화는 즉시 반영하지만 실제 체결 대상과 아직 차례가 오지 않은 미래 벽은 FIFO 재생 스냅샷으로 고정한다. 빨간 테두리는 144ms로 대상 가격에 도착하고 그 뒤에만 잔량 막대가 줄어든다. 이 표현 계층은 상태 펄스와 별개다.

- 최신 영웅문 실측 반영은 다음과 같다. 한 슬롯의 집계 체결은 큐에서 한 번만 소모하고 충분한 수량이면 정확한 합의 7~12개 표시 체결로 나뉘며 약 65%가 1~5주다. 10주 배수 스냅은 없고, 체결강도·거래대금은 자식 수량을 합산하되 가속 표본은 고유 `(시장분, 미세구조 프레임)`으로 센다. 보합 방향은 장기 50:50이다. 일반 이벤트 깊이는 46/32/22로 선택한 뒤 평온 55%·급변 78% 게이트를 적용하고, 비구조벽 깊이는 최우선이 얇고 5~6호가까지 두꺼워진다. 고정 5틱 미시 벽은 없으며 절대가격·심리가격 해시로 산발 생성한다. 모든 호가 행은 전일종가 대비 등락률을 표시하고 숫자는 즉시, 막대·중앙 테두리는 144ms로 갱신한다. 20억원 이상은 연속 양수 호가, 20억원 미만은 결정론적 공백 정책을 유지한다.

## 5. 최신 검증 상태

2026-08-01 현재 미래톡 9명 MBTI 채팅, 제6기 10명 일일 투자 결과·동기 대여와 여학생 8명 호감도 기능의 관련 검증과
Flutter Web·Vinext 릴리스 빌드는 **완료(PASS)**다.

- `flutter analyze`: PASS, issue 0
- 미래톡 상태·9명 차별화·일일 제한·저장·390×844 대화·360×800 허브 진입: 7/7 PASS
- 저장·관계·투자 결과·직접 하루 진행 묶음: 45/45 PASS
- v24 레거시 마이그레이션 표적 회귀: 1/1 PASS
- 일일 투자 정산·대여·상환·저장: 7/7 PASS
- 투자 결과 화면·직접 하루 진행: 7/7 PASS
- 관계 상태·저장·해금: 6/6 PASS
- 관계 화면: 2/2 PASS
- 전체 `game_engine_test.dart`와 `layout_regression_test.dart`에는 이번 범위와 무관한 기존 시장 날짜·호가 실패가 남아 있음
- Flutter Web release와 Vinext production build: PASS
- 웹 호스트 회귀 `pnpm test`: 9/9 PASS
- ESLint `pnpm run lint`: PASS
- `git diff --check`: PASS
- `flutter_app/build/web` ↔ `public/play`: 363/363개, SHA-256 불일치 0
- `main.dart.js`: 5,061,476바이트, SHA-256 `B831360ED9627E4C7CD964F6A5921B4B15EDBA256B078D3320767999049257E3`

## 6. 다음 행동

가장 먼저 할 일:

1. Android 실기기와 모바일 Web의 360×800·390×844에서 주식 10+10·인라인
   주문 슬라이드·테두리 세로 이동, `1년 저개입 진행`, 장부의 다섯 자산·총부채
   두 열 배치를 한 번에 확인하고 프레임 시간·터치 오입력을 기록한다.

기능 백로그는 이 문서에 복사하지 않고 `GAMEPLAY_GAPS.md`만 갱신한다.

## 7. 핵심 파일

| 파일 | 역할 |
| --- | --- |
| `flutter_app/lib/main.dart` | 앱 상태, 저장, 하루 전환과 신문 |
| `flutter_app/lib/game/game_state.dart` | v24 저장 모델과 관계·미래톡·일일 투자·대여채권·사업 자산·미지급금 합산 |
| `flutter_app/lib/game/phone_messenger_state.dart` | 제6기 9명 연락처·인물별 답장·읽음·일일 대화 횟수 저장 |
| `flutter_app/lib/phone_messenger_screens.dart` | 미래톡 채팅 목록·좌우 말풍선·추천 답장·자유 입력 UI |
| `flutter_app/lib/game/cohort_investment_state.dart` | 동기 9명 계좌·제6기 10명 결과·대여·상환 저장 |
| `flutter_app/lib/cohort_investment_screens.dart` | 15:00 투자 결과 10행 표와 하루 1회 대여 UI |
| `flutter_app/lib/game/relationship_state.dart` | 여학생 8명 호감도·단계·장면·최근 선택 저장 |
| `flutter_app/lib/relationship_screens.dart` | 관계 목록과 하루 종료 대화·데이트 UI |
| `flutter_app/lib/game/game_engine_corporate_actions.dart` | 분할·배당·합병·분사 기업행동 |
| `flutter_app/lib/game/game_engine_story_decisions.dart` | 연구·경영권·캠페인 선택 결과 |
| `flutter_app/lib/game/game_engine.dart` | 날짜·거래·경제·부동산·사업 일일 훅·마이그레이션 |
| `flutter_app/lib/game/market_era_events.dart` | 주식과 동일한 공통 실물경제 사건 객체의 정규 내보내기 |
| `flutter_app/lib/game/world_economy.dart` | 공통 사건의 자산별 투영·다음날 공개·활성 원인 보존·14권역 중앙 매핑 |
| `flutter_app/lib/game/stable_hash.dart` | Flutter VM·Web 공통 31비트 정확 FNV 곱셈 |
| `flutter_app/lib/game/business_state.dart` | 점포·지역·개점 기준 임대료지수·6축 정책·손익·사건 저장 모델 |
| `flutter_app/lib/game/business_districts.dart` | 상권 v1 레거시와 v2 공통 경제 투영, 32개 실제 상권·7국면·49개 지역 사건 |
| `flutter_app/lib/game/business_simulation.dart` | 사업 v1/v2 보존·신규 v3, 18업종·6개 미시 입지·실제 상권의 매물·손익·투자·사건 |
| `flutter_app/lib/game/business_engine.dart` | 공용 견적·회사 통장·월 정산·미지급·폐업·실제 지역 부동산 연결 |
| `flutter_app/lib/business_management_screen.dart` | 동네상권넷 5탭과 공통경제 공개 이력·상권판세·상세 운영 UI |
| `flutter_app/test/business_districts_test.dart` | 상권 카탈로그·공통 경제 단회 반영·비누설·매핑·레거시 테스트 |
| `flutter_app/test/business_simulation_test.dart` | 월 고정 매물·상권별 손익·사건·저장 결정론 테스트 |
| `flutter_app/test/business_engine_test.dart` | 인수·실제 지역 연결·정책·투자·월 정산·강제폐업 테스트 |
| `flutter_app/test/business_ui_test.dart` | 두 화면 크기·텍스트 1.2의 5탭·상권판세·인수·운영 UI 테스트 |
| `flutter_app/test/shared_economy_stock_export_test.dart` | 정규 주식 객체 재사용·정렬·순수 시장구조 제외 |
| `flutter_app/test/world_economy_test.dart` | 결정론·다음날 전파·분류·상한·14권역 매핑 |
| `flutter_app/test/shared_economy_cross_asset_test.dart` | 같은 ID의 주식→상권 v2→부동산 v4 단회 전파와 레거시 격리 |
| `flutter_app/lib/game/fictional_market.dart` | 50개 기업과 2000~2026 월드 |
| `flutter_app/lib/game/order_book.dart` | 방향별 잔량, 가격 전이, 부분체결 |
| `flutter_app/lib/stock_market_screen.dart` | 시장 UI, 시간 진행, 호가 세션 |
| `flutter_app/lib/game/market_liquidity_zones.dart` | 구조 지지·저항과 공백 |
| `flutter_app/lib/game/market_technical_levels.dart` | 5·20·60주 기술선 |
| `flutter_app/lib/game/world_bootstrapper.dart` | 시작·이어하기의 주식·부동산·공통 경제 예열과 비누설 검증 |
| `flutter_app/lib/game/real_estate_world.dart` | v1~v3 고정 재현, 신규 v4 공통 경제 투영·매물·권역·사건 |
| `flutter_app/lib/game/real_estate_financing.dart` | 대출·강제처분과 v4 유동성 기반 보유자 매각 대기 |
| `flutter_app/lib/game/real_estate_rental.dart` | 임대·보증금·세입자 사건 |
| `flutter_app/test/real_estate_world_economy_bridge_test.dart` | v3 지문 보존과 v4 숫자 교체·지역 사건·유동성 기간 |
| `flutter_app/lib/asset_spending_screen.dart` | 부동산 v4 공통 경제 사건 UI |
| `flutter_app/lib/rider_mini_game.dart` | 잼민 라이더 |
| `flutter_app/assets/images/minigames/rider_hero_pixel_v1.png` | 라이더 주인공 픽셀 스프라이트 |
| `flutter_app/web/index.html` | 모바일 높이와 데스크톱 390×844 프레임 |
| `flutter_app/web/stock-test.html` | 저장과 격리된 주식 전용 테스트 리다이렉트 |
| `flutter_app/lib/game/game_persistence.dart` | 5슬롯 저장·복구 |
| `scripts/build-flutter-web.mjs` | Flutter Web release 동기화 |
| `app/editor/page.tsx` | 대사·장면 편집, 화자별 포즈, 배경 선택과 새 장면 작성 |
| `app/editor/background-catalog.ts` | 편집기 배경 카탈로그 |
| `app/api/dialogue/build/route.ts` | 대사 JSON 저장과 Flutter Web 빌드 API |
| `flutter_app/lib/visual_novel_onboarding.dart` | 편집본 배경·동적 장면 수를 적용하는 프롤로그 런타임 |

## 8. 문서 역할

| 문서 | 한 가지 역할 |
| --- | --- |
| `AGENTS.md` | 절대 작업 규칙 |
| `DOCUMENTATION_INDEX.md` | 현행·검증·레거시 문서 우선순위 |
| `HANDOFF.md` | 새 채팅용 현재 상태·작업 트리·다음 행동 |
| `PROJECT_GUIDE.md` | 제품 구조·실행·필수 회귀 |
| `DECISIONS.md` | 현재 채택된 설계 결정 |
| `WORK_LOG.md` | 현재 완료 기능과 날짜·범위가 붙은 검증 기록 |
| `GAMEPLAY_GAPS.md` | 아직 남은 범위만 |
| `BALANCE_NOTES.md` | 현재 경제·시장 수치 |
| `DATA_SOURCES.md` | 출처·라이선스·가상화 경계 |
| `CONTENT_GUIDE.md` | 사건 작성 문법 |
| `ART_STYLE_GUIDE.md` | 캐릭터·배경·미니게임 아트 규격 |
| `ORPHANAGE_STORY_REBOOT.md` | 미래양성원 6기 리부트 정규 설정 |
| `PROTAGONIST_AGE_LINE.md` | 14~19살 실제 나이와 SEED 01~06 성장선 |
| `ORPHANAGE_WEBNOVEL_PROLOGUE.md` | 개막부 전체 원고 |
| `ART_STYLE_AUDIT.md` | 확정 화풍과 자산 세트 승인 상태 |
| `DIALOGUE_EDITOR_GUIDE.md` | 대사·장면·배경 편집과 게임 빌드 |

## 9. 검증 명령

```powershell
Push-Location flutter_app
flutter pub get
flutter analyze
Get-ChildItem test\*_test.dart | ForEach-Object { flutter test $_.FullName }
Pop-Location
npm test
npm run lint
npm run build:release
git diff --check
```

27년 월드 생성 테스트는 메모리를 많이 사용하므로 Flutter 테스트는 파일별로
순차 실행한다. 실기기 확인은 개발 서버를 `0.0.0.0:8000`에 열고 같은 Wi-Fi의
휴대폰에서 `http://<PC 내부 IP>:8000/play/index.html`로 접속한다. 주식만
검증할 때는 `http://<PC 내부 IP>:8000/play/stock-test.html`을 사용한다. 이
경로는 실제 저장과 격리되고 새로고침 때 초기화된다. 과거의 특정 내부 IP나
`?build=` 쿼리를 영구 문서 기준으로 복사하지 않는다.

## 2026-07-30 다중 호가 리셋·10주 고착 수정

- 여러 완전 소진 가격이 같은 펄스에 모두 다시 생기던 경로를 분리했다. 단일 소진은
  다음 펄스에 작은 큐를 허용하지만, 경쟁하는 여러 0 큐는 그 펄스에서 선택된
  가격만 재유입한다.
- 일반 큐의 첫 재유입은 기존 목표 바로 아래까지 한 번에 차오르지 않고 목표의
  최대 45%로 제한한다. 이후에도 선택된 펄스만 거래대금·기관·연기금 가중으로
  성장하며 복구 목표를 건너뛰거나 복구 중 구조벽으로 승격하지 않는다.
- 분 경계에서 전량 체결된 가격은 예전 ask/bid 벽을 되살리지 않는다. 같은
  절대가격에 반대 방향의 새 일반 큐를 16~28% 결정론적 초기 비율로 만들고,
  구조벽 붕괴 가격은 기존 벽/strength 기반 일반 회복 상한 아래에서만 커진다.
- 시장 화면 재진입은 10+10 절대가격을 유지한다. pending FIFO 최우선 매도·매수
  두 행 외에는 매도·매수 대표 벽 각 한 행만 추가로 바뀔 수 있다. 대표 절대가격은
  고정하고 개별 펄스 2% 이하, 재진입 누적 5% 이하이며 다른 깊은 행 변화는 실패다.
- 최종 검증: 주문장·미시구조 87/87, `widget_test.dart` 71/71,
  `flutter analyze` issue 0, `npm test` 8/8, `npm run lint` PASS,
  Flutter Web release + Vinext production build PASS. `public/play/main.dart.js`는
  4,882,389바이트, SHA-256
  `E5E611CA1DF68D86ED1737AECC5297A5D60A1D9953EDE7864C6846C5ED8C0061`이다.

## 2026-07-30 취소 표시 제거·0주 즉시 통과·가시 벽 호흡

- 비체결 취소·정정은 canonical 잔량 숫자에만 반영한다. 전체·압축 호가의
  `취소 N` 플래시와 감소 꼬리표를 제거하고 취소-only 패킷을 FIFO 재생에
  넣지 않는다. 실제 체결 감소와 신규 유입 증가는 기존 표시를 유지한다.
- 전량체결 step은 테두리 도착 프레임에 숫자 0 행을 만들지 않고 즉시 다음 양수
  호가로 진행한다. 첫 step·마지막 step 모두 0주 drain/final hold를 생략한다.
  다만 같은 절대가격에 새 양수 주문이 즉시 들어오면 그 가격은 다시 보일 수 있다.
- 매 새 호가 펄스에서 화면 안 10+10의 매도 또는 매수 대표 벽 한 줄만 수급한다.
  방향은 슬롯마다 번갈아 거래가 활발할수록 같은 게임분 안에서 더 자주 움직인다.
  매도·매수 대표 절대가격은 각각 고정하고 화면 밖 이탈·0주·구조 붕괴·공백일
  때만 교체한다. 플래그 벽이 없으면 해당 방향의 최대 가시 잔량을 사용한다.
- 기관·연기금·미세 흐름에 따라 대표 벽이 유입 또는 취소된다. 한 펄스 실제 이동
  상한은 평시 1.2%·급변 2.0%이고 작은 벽은 최소 1주씩 움직인다. 다른 정상 벽은
  그대로 두며 0주·구조 붕괴·공백 벽은 부활시키지 않는다.
- 인라인 주문 슬라이드 중에도 같은 호가 레일 `State`, 매도 10행·매수 10행과
  절대가격을 유지한다. 재생 펄스 한 번에 큰 잔량 한 행만 2% 이내로 바뀌고,
  중앙 빨간 테두리는 가로 슬라이드와 분리되어 세로로 최대 한 행만 이동한다.

## 2026-07-30 모바일 MTS 영상 대조

10분 이상 다음 세로형 레퍼런스를 프레임 단위로 비교했다.

- 로컬 `Downloads/영웅문.mp4` 608×1080, 27.84초
- 사용자 Shorts: https://www.youtube.com/shorts/3tzsfnoCPz8
- 모바일 호가주문 참고: https://www.youtube.com/watch?v=rduCyZO8LYQ
- 영웅문S 호가주문 참고: https://www.youtube.com/watch?v=QrjpObRKVBI
- 토스 모바일 호가 참고: https://www.youtube.com/watch?v=FSxyTHgMwZQ

공통 기준은 종목 헤더·현재가·호가 문맥을 유지한 같은 화면 분할, 매수/매도 탭
전환 시 호가 레일 보존, 가격 탭→주문 가격 전달, 0주 행 미표시, 한두 행의 국소
잔량 갱신, 주문 키패드/바텀 패널이 떠도 호가를 없애지 않는 것이다. 이번 변경으로
흰 화면 재마운트, 0주 ghost, 내부 플래그가 없는 큰 벽의 정지, 부분체결 벽의
영구 고착, 전량체결 마지막 hold를 회귀로 닫았다.

남은 차이는 실제 MTS의 숫자 키패드·호가 드래그/롱프레스·햅틱·오주문 방지 확인,
체결/미체결/잔고의 더 높은 정보 밀도다. 현재 게임은 `+/-`, 프리셋, 간소화된
정정/취소를 사용하므로 이는 후속 모바일 UX 범위이며 호가 엔진 버그와 구분한다.

- 최종 검증: 주문장·미시구조 88/88, `widget_test.dart` 71/71,
  합계 159/159, `flutter analyze` issue 0, `npm test` 8/8,
  `npm run lint`, Flutter Web release, Vinext production build PASS.
- `build/web` ↔ `public/play` 243/243, SHA-256 불일치 0.
  `main.dart.js`는 4,882,863바이트, SHA-256
  `EAA47483DF8814C5710E6D261B21DB156A07EC89FCE4EFE15F88E05B729BAE83`다.

## 2026-07-30 장기 적자·1년 저개입·통합 장부

- 기준 10년 재생의 적자 편의점은 이전에는 10년 뒤에도 `struggling`, 최근 60개
  손익이 전부 적자, 사업 순현금흐름 -1,309,785,098원이었다. v3 구조적 실패
  규칙 적용 뒤 2001-01-01에 12개월 정산으로 청산되고 순현금흐름은
  -131,832,839원, 미지급금·장부가치·중단 날짜는 0이 됐다.
- `1년 저개입 진행`은 중요뉴스를 버리지 않고 신문함에 보관하면서 뉴스 정지만
  해제한다. 안건과 캠페인 종료 정지는 유지한다.
- 통합 장부는 기존 순자산식을 자산군별로 분해한 표시이며 저장 마이그레이션은 없다.
- 최종 검증은 위 5절 수치와 같고 릴리스 산출물 243개가 공개 폴더와 모두 일치한다.
## 2026-07-30 실시간 양방향 대표 벽 수급

- 첫 펄스·분 단위 교대만 쓰던 벽 호흡을 모든 새 호가 펄스로 확대했다. 거래가
  많은 종목은 같은 게임분에도 매도·매수 대표 벽이 번갈아 움직인다. 직전 수정
  방향을 스냅샷에 기억하므로 10× 배속·누락 프레임에서도 같은 쪽이 연속되지
  않고, 1펄스 종목도 각 방향이 2게임분 안에 한 번씩 갱신된다.
- 후보는 예비 호가가 아닌 실제 표시 10+10으로 제한했다. 매도·매수 대표 가격을
  스냅샷에 고정해 빠른 catch-up과 경로 재진입에서도 같은 방향 여러 행을 바꾸지 않는다.
- 18주 벽은 펄스마다 1주씩 변하면서 `boundaryBidPrice`, 마지막 체결가, 빨간
  테두리·테이프·sweep를 그대로 유지하는 회귀로 고정했다.
- 최종 검증: 주문장 58/58 + 미시구조 32/32 = 90/90, 전체 위젯 71/71,
  합계 161/161, `flutter analyze` issue 0, `npm test` 8/8, `npm run lint`,
  Flutter Web release와 Vinext production build PASS. `build/web`와
  `public/play`는 243/243개, SHA-256 불일치 0이다. `main.dart.js`는
  4,887,165바이트, SHA-256
  `13D10DBD69C12DDAD00A7F681440B8588C4F48EB988E8FB5C48477665BE20E4B`다.

## 2026-07-30 24,500원 돌파 뒤 체결 경계 비되감기 확인

- 24,000원에서 24,500원 매도벽까지 50원 단위 모든 ask를 실제 소진하는 정확한
  회귀를 추가했다. 같은 게임분에 목표가만 24,300원으로 바뀌면 현재가·중앙
  테두리·최우선 매수는 24,500원에 남고 sweep·테이프는 새로 생기지 않는다.
- 24,500원 매수호가가 부분 체결이면 경계는 그 자리에 남는다. 24,300원 체결은
  24,500→24,450→24,400→24,350→24,300 순서의 실제 bid 체결이 있을 때만
  허용한다. 중간 호가가 실제로 빈 초저유동 장의 가격 갭은 정상 예외다.
- 검증: 주문장 58/58 + 미시구조 32/32 = 90/90, 10× FIFO와 깊은 스윕
  위젯 회귀 2/2, lutter analyze issue 0. 제품 코드는 추가 변경하지 않았고 회귀와
  문서만 보강했으므로 기존 릴리스 SHA-256은 유지된다.

## 2026-07-30 경영권 시스템 1차

- 신규 경영권 기회는 2005-01-01 이후 한빛전자부품으로 열린다.
  12만원/18%+관찰권, 22만원/34%+2/7석, 30만원/55%+4/7석이며
  소수·주요 지분은 90일 후 추가 취득할 수 있다.
- 취득·출자금은 회사 통장에서 빠지지만 같은 금액을
  `CompanyState.investmentBookValue`로 잡아 순자산을 보존한다.
  배당은 양수 월영업이익의 30%에 경제적 지분율을 곱한다.
- 55% 취득 후 대표 체제와 첫 공장 전략을 연속 안건으로 고른다.
  미래양성원 운영 자문은 직원 수에 포함하지 않는다.
- 조직 화면의 `controlled-company-governance-card`에서 지분, 의결권,
  이사회, 투자 장부가, 월 영업이익, 예상 배당, 운영 책임을 확인한다.
- v21로 올리고 누락 필드를 안전 기본값으로 복원한다. 과거
  한빛통신 경영권 저장은 레거시 호환이며 신규 대상은 한빛전자부품이다.
- 1차 이후 남은 매각·희석·경영권 상실·복수 자회사·연결재무·반복
  이사회는 `GAMEPLAY_GAPS.md`의 `경영권 시스템 2차`가 정규 백로그다.
- 최종 검증: Flutter 47/47 테스트 파일 전체 PASS(엔진 87/87,
  전체 위젯 71/71, 레이아웃 11/11 포함), `flutter analyze` issue 0,
  `npm test` 8/8, `npm run lint`, Flutter Web release와 Vinext production
  build PASS. `build/web`와 `public/play`는 243/243개, SHA-256 불일치
  0이다. `main.dart.js`는 4,903,823바이트, SHA-256
  `793A2F1F24C126C038741D601E5759EE57C88E260ED223DDC8FC67CC228B949A`다.
